#!/bin/sh

case "$(basename $0)" in 
  sb-cpu) color=#C14652 ;;
  sb-memory) color=#c27edd ;;
  sb-clock) color=#ECBE60 ;;
  sb-swap) color=#ffc517 ;;
  sb-disk) color=#86C154 ;;
  sb-volume) color=#99c1e9 ;;
  *) color=#FFFFFF ;;
esac
